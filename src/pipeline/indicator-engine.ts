/**
 * Re-export from core — indicator engine has moved to src/core/indicator-engine.ts.
 * This file exists for backwards compatibility with existing imports.
 */
export { computeIndicators, seedIndicatorState as seedState, resetVWAP } from '../core/indicator-engine';

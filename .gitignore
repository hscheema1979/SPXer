# Dependencies & build
node_modules/
dist/
build/

# Environment & secrets
.env

# Database & market data (112GB+ — never track)
data/
*.parquet

# Logs (runtime output)
logs/

# Tool/IDE state
.omc/
.claude/
.pi/
.ultrapilot/
.worktrees/

# Autoresearch temp files
.autoresearch-results.tsv
.autoresearch-results.tsv.backup.*
autoresearch-config.json
.*-matrix-results.tsv
.*-matrix.log
.*-matrix-stdout.log
.*-autoresearch*.log
.*-autoresearch*.tsv

# Test/replay output
test-results/
results/

# Scripts
ecosystem.config.js.pre-basket-bak
rclone-*.deb

# Screenshots (keep in docs/ if needed, not root)
*.png

# Stray DB files in root
/spxer.db

# Local backup dumps (multi-GB sweep-output snapshots — never track)
backups/

# Generated sweep/replay output DATA (regenerated by the engines; read from disk
# at runtime, not source). Code lives in scripts/; these are large artifacts that
# exceed GitHub's 100MB file limit. Keep them out of git.
scripts/autoresearch/output/*.json
scripts/autoresearch/output/etf-long-sweep-*.json
scripts/autoresearch/output/cache/
scripts/autoresearch/output/iron-trades/
scripts/autoresearch/output/iron-trades-fresh/
scripts/autoresearch/output/spread-trades/

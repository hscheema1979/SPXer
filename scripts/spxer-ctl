#!/usr/bin/env bash
# spxer-ctl — shell wrapper for the SPXer CLI
# Usage: ./scripts/spxer-ctl <command> [args]
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec npx tsx "${SCRIPT_DIR}/spxer-ctl.ts" "$@"
